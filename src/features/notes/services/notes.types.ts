export type NoteDto = {
  id: number;
  title: string;
  excerpt: string;
  author: string;
  updated: string;
};

export type Note = {
  id: number;
  title: string;
  excerpt: string;
  author: string;
  initial: string;
  color: string;
  updated: string;
};
